/* * Author: Ethan Epstein * Email: ethanepstein@utexas.edu * Date: Spring 2020 * License: Public Domain*/#include "GripTest.h"#define UP 0#define DOWN 1#define OKAY 2#define FORCE A0/* * initialize pins as digital input pins for the three buttons*/void setup() {    pinMode(UP, INPUT_PULLUP);  pinMode(DOWN, INPUT_PULLUP);  pinMode(OKAY, INPUT_PULLUP);  }/* * poll pins. Upon press, call runTest() function to conduct a test*/void loop() {    if(!digitalRead(UP) || !digitalRead(DOWN) || !digitalRead(OKAY)){    runTest();  }  }/* * Initialize dynamometer input and display output, and create and operate a GripTest object*/void runTest() {  while(!digitalRead(OKAY)){} //Debounce  GripTest myTest;  while(!digitalRead(OKAY)){} //Debounce  myTest.test();  myTest.displayResults();  delete &myTest;}